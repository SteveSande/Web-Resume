import MyMenu from "./navigation-menu";

function Header() {
  return (
    <header className="bg-blue-100 flex justify-around p-[50px]">
      <MyMenu />
    </header>
  );
}

export default Header;
